# Minimal runtime deps for the FastAPI serverless function on Vercel
# Keep this list lean to stay under the 250 MB unzipped limit

fastapi>=0.104.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

openai>=1.0.0

# LangChain core pieces actually used by the codebase
langchain-core>=0.1.0
langchain-community>=0.0.20
langchain>=0.1.0
langchain-text-splitters>=0.0.1

# Vector backend
pymilvus>=2.3.0

# LangGraph (without postgres extras)
langgraph>=0.1.0
langgraph-checkpoint>=1.0.0

requests>=2.31.0
typing-extensions>=4.8.0